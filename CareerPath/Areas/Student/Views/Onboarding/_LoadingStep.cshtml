<div class="text-center py-5">
    <div class="mb-4">
        <!-- Animated icon -->
        <div class="position-relative d-inline-block">
            <svg class="text-primary" width="80" height="80" fill="currentColor" viewBox="0 0 24 24" style="animation: pulse 2s ease-in-out infinite;">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zM7 11h2v2H7v-2zm4 0h2v2h-2v-2zm4 0h2v2h-2v-2z"/>
            </svg>
            <div class="position-absolute top-50 start-50 translate-middle">
                <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
                    <span class="visually-hidden">Loading...</span>
                </div>
            </div>
        </div>
    </div>

    <h4 class="fw-bold mb-2">Analyzing Your Profile</h4>
    <p class="text-muted mb-4">
        Our AI is extracting key features from your data...
    </p>

    <!-- Progress Bar -->
    <div class="mx-auto mb-4" style="max-width: 24rem;">
        <div class="progress" style="height: 12px;">
            <div class="progress-bar progress-bar-striped progress-bar-animated" 
                 role="progressbar" 
                 style="width: 100%; background: linear-gradient(90deg, #325d88, #29abe0);">
            </div>
        </div>
    </div>

    <!-- Feature List -->
    <div class="mt-4">
        <div class="d-inline-flex flex-wrap gap-2 justify-content-center">
            <span class="badge bg-light text-muted border">Resume Parsing</span>
            <span class="badge bg-light text-muted border">GitHub Analysis</span>
            <span class="badge bg-light text-muted border">Skill Extraction</span>
            <span class="badge bg-light text-muted border">Profile Scoring</span>
            <span class="badge bg-light text-muted border">Gap Detection</span>
        </div>
    </div>
</div>

<style>
    @@keyframes pulse {
        0%, 100% {
            opacity: 0.3;
            transform: scale(0.95);
        }
        50% {
            opacity: 1;
            transform: scale(1);
        }
    }
</style>
